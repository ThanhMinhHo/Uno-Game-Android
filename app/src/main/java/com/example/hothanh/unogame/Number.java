package com.example.hothanh.unogame;

//Eventhough only number cards will be used - no special cards (Wild, Draw Two, etc) need to be implemented.
//However, for further extentions, it might require enum to make the character rather than number (0-9)
public enum Number
{
    ZERO,
    ONE,
    TWO,
    THREE,
    FOUR,
    FIVE,
    SIX,
    SEVEN,
    EIGHT,
    NINE
}
